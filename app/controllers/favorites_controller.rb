class FavoritesController < ApplicationController

#   def create
#     book = Book.find(params[:book_id])
#     favorite = current_user.favorites.new(book_id: user.id)
#     favorite.save
#     redirect_to user_path(book)
#   end

#   def destroy
#     book = Book.find(params[:book_id])
#     favorite = current_user.favorites.new(book_id: user.id)
#     favorite.destroy
#     redirect_to user_path(book)
#   end

end
